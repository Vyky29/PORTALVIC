# Secrets & local config (never commit)
.env
.env.*
!.env.example
*.local.env
database/local-vault/*.local.sql
working_ui/onboarding_portal_config.js

# Local password vault (data + copies — never commit, never deploy)
# Prefer _local/secrets/ (see bootstrap-local-data.ps1). Junction local-secrets/ is OK.
local-secrets/
_local/

# Vercel
.vercel

# OS / editor noise
.DS_Store
Thumbs.db
Desktop.ini

# Logs & temp
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.tmp_*
**/_tmp_*
.tmp_induction_repo/
supabase/.temp/

# Python
__pycache__/
*.py[cod]

# HR + payments source data + generated seeds (PII: never commit, never deploy)
# Canonical path: _local/hr and _local/payments (legacy folder names still ignored)
hr_source/
payments_source/
*.xlsx

# Node (Supabase CLI via package.json)
node_modules/
